# NanoBanana - AI 图像处理平台

基于 Flutter 开发的 AI 图像处理平台，提供专业级的图像处理和分析功能。

## 功能特性

- 🎨 现代化的 UI 设计
- 🌓 亮色/暗色主题切换
- 📱 完全响应式布局（支持移动端和桌面端）
- 🖼️ 图片上传和预览
- 🤖 AI 图像处理模拟
- 📊 实时处理结果展示

## 技术栈

- **Flutter** - UI 框架
- **GetX** - 状态管理和路由
- **ducafe_ui_core** - UI 组件库
- **image_picker** - 图片选择
- **Google Fonts** - 字体支持

## 项目结构

```
lib/
├── app/                    # 应用配置层
│   ├── routes/            # 路由配置
│   ├── theme/             # 主题配置
│   └── constants/         # 常量定义
├── core/                   # 核心功能层
│   ├── utils/             # 工具类
│   └── extensions/        # 扩展方法
├── data/                   # 数据层
│   └── models/            # 数据模型
├── modules/                # 功能模块
│   └── home/              # 首页模块
│       ├── controllers/   # 控制器
│       ├── views/         # 视图
│       └── widgets/       # 页面组件
├── shared/                 # 共享层
│   └── widgets/           # 通用组件
└── main.dart              # 应用入口
```

## 安装步骤

1. 确保已安装 Flutter SDK (>=3.2.0)
2. 克隆项目到本地
3. 安装依赖：

```bash
flutter pub get
```

## 运行项目

```bash
# 开发模式运行
flutter run

# 构建 APK（Android）
flutter build apk

# 构建 iOS
flutter build ios
```

## 开发规范

项目遵循严格的 Dart/Flutter 开发规范，详见 `.cursorrules` 文件。

核心原则：
- 始终声明类型，避免使用 dynamic
- 使用 GetX 进行状态管理
- 保持代码简洁和可维护性
- 遵循 SOLID 原则

## 许可证

Copyright © 2025 NanoBanana. All rights reserved.

